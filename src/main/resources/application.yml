micronaut:
  application:
    name: joinOracleProblem
datasources:
  default:
    url: jdbc:oracle:thin:@localhost:49161/XE
    driverClassName: oracle.jdbc.OracleDriver
    username: system
    password: oracle
netty:
  default:
    allocator:
      max-order: 3
